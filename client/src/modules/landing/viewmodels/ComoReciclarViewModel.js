/**
 * ComoReciclarViewModel
 * ----------------------
 * ViewModel de la página "Cómo reciclar".
 * No tiene estado reactivo complejo: la página es mayormente estática.
 */

import { BaseViewModel } from '../../../core/BaseViewModel.js'

export class ComoReciclarViewModel extends BaseViewModel {
  /**
   * Inicializa el estado del ViewModel.
   */
  _initState() {
    // Página estática, sin estado dinámico por ahora
  }
}
