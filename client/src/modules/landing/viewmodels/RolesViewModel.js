/**
 * RolesViewModel
 * ---------------
 * ViewModel de la página "Selección de roles y Modificar usuario".
 * Página estática sin estado dinámico por ahora.
 */

import { BaseViewModel } from '../../../core/BaseViewModel.js'

export class RolesViewModel extends BaseViewModel {
  /**
   * Inicializa el estado del ViewModel.
   */
  _initState() {
    // Página estática, sin estado dinámico por ahora
  }
}
