/**
 * ComoCanjarViewModel
 * --------------------
 * ViewModel de la página "Cómo canjear recompensas".
 * Página estática sin estado dinámico por ahora.
 */

import { BaseViewModel } from '../../../core/BaseViewModel.js'

export class ComoCanjarViewModel extends BaseViewModel {
  /**
   * Inicializa el estado del ViewModel.
   */
  _initState() {
    // Página estática, sin estado dinámico por ahora
  }
}
